import { RegisterPageView } from "@/components/pages/RegisterPageView";

export const metadata = {
  title: "Register & Onboarding - EVVAI Pharmaceuticals",
  description: "Register for an EVVAI customer account or apply for B2B distributor network onboarding.",
};

export default function RegisterPage() {
  return <RegisterPageView />;
}
